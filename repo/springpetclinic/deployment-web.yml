apiVersion: apps/v1
kind: Deployment
metadata:
  name: springpet-clinic
  labels:
    app: springpet-application
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springpet-application
  template:
    metadata:
      labels:
        app: springpet-application
    spec:
      containers:
        - name: my-container
          image: "svikram01/springpet-docker:latest"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 9030
              protocol: TCP
            - name: https
              containerPort: 8082
              protocol: TCP
